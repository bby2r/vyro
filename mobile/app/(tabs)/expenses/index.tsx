import { Text, View } from 'react-native';

export default function ExpensesIndex() {
  return (
    <View style={{ flex: 1, padding: 16, backgroundColor: '#0d1117' }}>
      <Text style={{ color: '#c9d1d9' }}>expenses tab — index reached</Text>
    </View>
  );
}
