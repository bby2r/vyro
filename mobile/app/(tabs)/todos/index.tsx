import { Text, View } from 'react-native';

export default function TodosIndex() {
  return (
    <View style={{ flex: 1, padding: 16, backgroundColor: '#0d1117' }}>
      <Text style={{ color: '#c9d1d9' }}>todos tab — index reached</Text>
    </View>
  );
}
